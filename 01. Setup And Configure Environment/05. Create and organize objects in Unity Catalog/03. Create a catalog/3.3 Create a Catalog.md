## Create a Catalog

Creating a catalog is the foundational step in organizing your data estate. It establishes the top-level container for all subsequent schemas, tables, and volumes. To perform this action, you must be a **metastore admin** or possess the `CREATE CATALOG` privilege.

### Automatic System Objects

Upon creation, Unity Catalog automatically generates two essential schemas within the new catalog:

1.  **`default`**: A standard schema available for general use if no other schema is specified.
2.  **`information_schema`**: A read-only schema containing system metadata about all objects within the catalog, such as table definitions and column types.

### Method 1: Using SQL

SQL provides a scriptable, reproducible way to create catalogs. The `IF NOT EXISTS` clause ensures idempotency, preventing errors if the script is run multiple times.

```sql
CREATE CATALOG IF NOT EXISTS dev_catalog 
COMMENT 'Development environment for data engineering experiments';
```

> [!NOTE]
> **The Power of Comments**
> Always include a `COMMENT` when creating objects. These descriptions appear in Catalog Explorer and help users understand the purpose of the catalog without needing to consult external documentation.

### Method 2: Using Catalog Explorer

For a visual approach, you can create catalogs directly through the Azure Databricks UI:

1.  Navigate to **Catalog** in the left sidebar.
2.  Select **Create catalog**.
3.  Enter a unique name and select **Standard** as the type.
4.  (Optional) Specify a custom **managed storage location** if you need physical separation from the metastore default.
5.  Select **Create**.

### Workspace Bindings and Access Control

By default, a new catalog is accessible to **all workspaces** attached to the metastore. While this simplifies initial setup, it poses a risk for production environments.

> [!IMPORTANT]
> **Restricting Production Access**
> For production catalogs, you should explicitly configure **workspace bindings** to restrict access to only production-authorized workspaces. This prevents developers in `dev` or `test` workspaces from accidentally querying or modifying live production data, enforcing a critical security boundary at the infrastructure level.


<!-- path-normalized -->