## Create Streaming Tables

Streaming tables represent a paradigm shift from batch-oriented processing to continuous, incremental data ingestion. While materialized views recompute results over bounded datasets, streaming tables use **streaming semantics** to process only the new rows that arrive after the last refresh. This makes them exceptionally efficient for large, ever-growing datasets where reprocessing historical data would be prohibitively expensive.

Every streaming table is backed by a Delta table and managed by a dedicated serverless pipeline that Unity Catalog creates automatically. This abstraction allows you to define streaming logic using standard SQL without managing complex infrastructure.

### Key Characteristics

*   **Incremental Processing:** Reads only new data since the last checkpoint, avoiding redundant computation.
*   **Exactly-Once Semantics:** Ensures that each record is processed exactly once, even in the event of failures or retries.
*   **Serverless Management:** The underlying pipeline is fully managed by Azure Databricks, reducing operational overhead.

### Creating a Streaming Table

To create a streaming table, use the `CREATE OR REFRESH STREAMING TABLE` statement. The `STREAM` keyword is critical—it tells the engine to treat the source as an unbounded stream rather than a static table.

#### Example: Ingesting Raw Files with Auto Loader

```sql
CREATE OR REFRESH STREAMING TABLE bronze_orders 
SCHEDULE EVERY 1 HOUR 
AS 
SELECT * 
FROM STREAM read_files( 
    '/Volumes/prod_catalog/landing/raw_files/orders/', 
    format => 'json' 
);
```

In this example, the table ingests new JSON files from a landing zone every hour. Because it uses `read_files` with the `STREAM` keyword, it only processes files that have arrived since the last run.

### Trigger Strategies: Schedule vs. Event-Driven

Streaming tables offer two primary ways to initiate refreshes, allowing you to match the behavior to your data arrival patterns.

| Strategy | Syntax | Best For |
| :--- | :--- | :--- |
| **Scheduled** | `SCHEDULE EVERY 1 HOUR` | Sources with predictable arrival times or when you want to batch small increments into larger, less frequent updates. |
| **Event-Driven** | `TRIGGER ON UPDATE` | Production pipelines where upstream data arrives unpredictably. The table refreshes automatically whenever its source tables are updated. |

#### Example: Event-Driven Transformation

```sql
CREATE OR REFRESH STREAMING TABLE silver_orders 
TRIGGER ON UPDATE 
AS 
SELECT order_id, customer_id, order_total 
FROM STREAM bronze_orders;
```

Using `TRIGGER ON UPDATE` eliminates the need to coordinate timing between pipelines. As soon as new data lands in `bronze_orders`, the `silver_orders` table begins processing it.

### Manual Refreshes

While automation is preferred, you can manually trigger a refresh at any time for testing or immediate data availability.

```sql
REFRESH STREAMING TABLE silver_orders;
```

### Streaming Tables vs. Materialized Views

Choosing between these two objects depends on the nature of your source data and your performance requirements.

> [!IMPORTANT]
> **When to Choose Streaming Tables**
> *   **Append-Only Sources:** When data only ever arrives as new rows (e.g., Kafka topics, IoT sensor logs, Auto Loader file ingestion).
> *   **No Full History:** When the source system does not retain full historical data, making full recomputation impossible.
> *   **Efficiency:** When you need to process massive volumes of data incrementally without the cost of scanning the entire dataset every time.

> [!NOTE]
> **When to Stick with Materialized Views**
> *   **Complex Aggregations:** When you need to recalculate summaries over the entire dataset (e.g., changing a business rule that affects historical totals).
> *   **Non-Append Sources:** When source data can be updated or deleted, requiring a full scan to ensure accuracy.

By leveraging streaming tables, you build medallion architectures that are not only scalable and cost-effective but also responsive to real-time data flows, ensuring your downstream analytics are always powered by the freshest possible information.


<!-- structure-verified -->