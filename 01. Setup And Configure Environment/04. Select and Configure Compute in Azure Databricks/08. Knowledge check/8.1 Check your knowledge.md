## Check your knowledge
Which compute type provides the fastest startup time and requires minimal management overhead?
Classic compute with instance pools
Serverless compute
SQL warehouse in classic mode
What is the primary limitation of serverless compute compared to classic compute?
Serverless compute cannot use Photon acceleration
Serverless compute doesn't support RDD APIs, R language, or JAR libraries
Serverless compute is more expensive than classic compute
When configuring node types for a workload with large joins and aggregations, which instance type is most appropriate?
Compute-optimized instances like F-series
Memory-optimized instances like E-series
Storage-optimized instances like L-series
What permission level allows users to attach notebooks and view metrics but prevents them from starting or stopping compute resources?
CAN MANAGE
CAN RESTART
CAN ATTACH TO
Which access mode enables secure group collaboration for workloads requiring RDD APIs or R language?
Standard access mode
No isolation shared access mode
Dedicated access mode with group assignment
When installing a Maven library on a cluster with standard access mode, what additional step is required?
The library must be stored in a Unity Catalog volume first
A metastore admin must add the Maven coordinates to the allow list
The cluster must be restarted after library installation
What is the primary advantage of using spot instances for worker nodes in job compute?
Spot instances provide higher performance than on-demand instances
Spot instances cost up to 90% less than on-demand instances
Spot instances start faster than on-demand instances
You must answer all questions before checking your work.
You must answer all questions before checking your work.


<!-- path-normalized -->