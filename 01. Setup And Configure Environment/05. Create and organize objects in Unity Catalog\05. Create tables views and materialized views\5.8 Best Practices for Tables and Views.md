## Best Practices for Tables and Views

Designing a robust data model in Unity Catalog requires more than just writing SQL; it demands a strategic approach to storage, performance, and governance. The decisions you make at the table and view level dictate the scalability, cost-efficiency, and security of your entire platform.

### 1. Prioritize Managed Tables

Unless you have a specific architectural requirement for external storage, **always start with managed tables**.

*   **Unified Governance:** Managed tables are fully integrated with Unity Catalog, ensuring that metadata, lineage, and access controls are automatically synchronized.
*   **Lifecycle Management:** When you drop a managed table, Unity Catalog cleans up both the metadata and the underlying files. This prevents "data rot"—the accumulation of orphaned files in cloud storage that incur costs but provide no value.
*   **Automatic Optimization:** Managed tables benefit from Databricks’ auto-optimization features, such as automatic compaction of small files and Z-Ordering, which significantly improve query performance without manual intervention.

> [!IMPORTANT]
> **When to Use External Tables**
> Reserve external tables for scenarios where data must remain accessible to non-Databricks systems or when you are ingesting legacy data that cannot be moved. For all new pipelines, managed tables are the superior choice for maintainability and security.

### 2. Enable Row Tracking for Incremental Processing

If your tables serve as sources for materialized views or streaming tables, enable **row tracking**. This feature assigns a unique, immutable identifier to every row, allowing downstream processes to identify exactly which records have changed.

```sql
ALTER TABLE sales_transactions 
SET TBLPROPERTIES (delta.enableRowTracking = true);
```

*   **Efficiency:** Row tracking enables **incremental refreshes** for materialized views, processing only changed data rather than rescanning the entire dataset.
*   **Cost Savings:** By avoiding full recomputation, you reduce DBU consumption and accelerate pipeline completion times.
*   **Irreversibility:** Note that enabling row tracking upgrades the Delta protocol version and cannot be reversed. Plan this configuration early in your table’s lifecycle.

### 3. Simplify View Hierarchies

Views are powerful tools for abstraction, but excessive layering can create a "spaghetti architecture" that is difficult to debug and optimize.

*   **Limit Depth:** Aim for shallow view hierarchies (no more than 2–3 layers). Deeply nested views force the query optimizer to resolve complex dependency chains, often leading to suboptimal execution plans.
*   **Document Intent:** Every view should have a clear `COMMENT` describing its business purpose and intended audience. This transforms your schema from a technical artifact into a self-documenting business asset.
*   **Performance Awareness:** Remember that standard views are computed on read. If a view involves heavy joins or aggregations, consider whether a materialized view would provide a better user experience.

### 4. Centralize Security with Dynamic Views

Avoid creating separate tables or views for different user groups (e.g., `sales_us`, `sales_eu`). Instead, use **dynamic views** to enforce row-level and column-level security within a single object.

*   **Single Source of Truth:** Dynamic views ensure that all users query the same underlying data, reducing the risk of inconsistency and duplication.
*   **Centralized Logic:** Security rules are defined once in the view definition, making them easier to audit and update than scattered permission sets across multiple objects.
*   **Combine with Permissions:** Pair dynamic views with Unity Catalog’s `GRANT` statements to create a defense-in-depth strategy. Use permissions to control *who* can see the view, and dynamic logic to control *what* they see inside it.

### 5. Choose the Right View Type for the Workload

The choice between standard and materialized views should be driven by the trade-off between **data freshness** and **query performance**.

| Feature | Standard View | Materialized View |
| :--- | :--- | :--- |
| **Data Freshness** | Real-time. | Eventual consistency (depends on refresh schedule). |
| **Performance** | Varies; computes on every query. | High; reads precomputed results. |
| **Storage Cost** | None. | Consumes storage for cached data. |
| **Best For** | Ad-hoc exploration, real-time dashboards. | Heavy aggregations, high-concurrency reporting. |

> [!NOTE]
> **Monitor Refresh Behavior**
> For materialized views, regularly monitor refresh logs to ensure they are using **incremental refresh** rather than full recomputation. A materialized view that performs a full scan on every refresh negates its primary performance benefit.

### 6. Adopt Liquid Clustering Over Partitioning

Traditional partitioning (e.g., `PARTITIONED BY (date)`) is rigid and prone to skew. **Liquid clustering** is the modern alternative for optimizing data layout in Delta Lake.

*   **Dynamic Optimization:** Liquid clustering automatically organizes data based on query patterns, co-locating related rows to minimize I/O.
*   **No Skew Issues:** Unlike partitioning, which can create massive "hot" partitions and tiny empty ones, liquid clustering adapts to data distribution changes over time.
*   **Ease of Maintenance:** You define clustering keys at creation, and Databricks handles the rest. There is no need to manually manage partition directories or worry about small file problems.

```sql
CREATE TABLE customer_orders (
    order_id BIGINT,
    customer_id BIGINT,
    order_date DATE
) CLUSTER BY (customer_id, order_date);
```

By applying these best practices, you build a data foundation that is not only performant and cost-effective but also secure and easy to maintain as your organization scales.

